# Deliberate legacy-feature probe, used only by the transitive-peer negative test.
[package]
name = "legacy-peer"
version = "0.1.0"
edition = "2021"
[features]
__boomerang_descriptor = []
__boomerang_payload = []
[dependencies]
vehicle-control = { path = "../vehicle-control", default-features = false }
