[package]
name = "vehicle-control"
version = "0.1.0"
edition = "2021"
[features]
broken-payload = []
broken-descriptor = []
controller-selected = []
sensor-selected = []
profile-config-probe = []
warning-diagnostic = []

[dependencies]
boomerang = { path = "../../../../../boomerang" }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(boomerang_facet, values("descriptor", "payload"))', "cfg(boomerang_cargo_config_probe)"] }
