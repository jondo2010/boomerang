[package]
name = "descriptor-duplicate-reaction"
version = "0.0.0"
edition = "2021"
publish = false

[features]
duplicate-mode = []

[dependencies]
boomerang = { path = "../../../../boomerang" }

[workspace]
